<div class="modal fade signedContractDeletion-modal-lg" data-backdrop="static" data-keyboard="true" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="row ">
                <div class="col-xs-offset-1">
                    <h1>{{ translate('confirmation') }}</h1>
                    <h3>{{ translate('delete_signed_contract_message') }} </h3>
                </div>
            </div>
            <div class="row lead">
                <div class="col-xs-offset-2 col-xs-9 ">
                    <div class="pull-right">
                        <button class="btn btn-default" id="cancelReservationBtn" onclick="$('.signedContractDeletion-modal-lg').modal('hide');">{{ translate('go_back') }}</button>
                        <button class="btn btn-danger save-btn" onclick="DeleteContract()">{{ translate('confirm') }}</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
