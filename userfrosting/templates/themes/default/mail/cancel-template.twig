{% block subject %}
    Unit {{ unit.rawabi_code }} Cancellation - {{ user.display_name }}
{% endblock %}

{% block body %}
    <br>
 <div style="font-size:medium; float: left" dir="ltr">
        The unit of rawabi_code {{ unit.rawabi_code }} has been cancelled by <b>{{ user.display_name }}</b>.
        <p style="font-size:medium;">Cancellation Reason: {{ cancelReason.reason }}</p>
    <h3>Cancelled Client details:</h3>
    <ul style="font-size: medium;">
        <li>Client name :{{ reservedUnit.customer_name }}</li>
        <li>Client mobile number : {{ reservedUnit.mobile }}</li>
        <li>Client address : {{ reservedUnit.customer_address }}</li>
        <li>LeadID : {{ reservedUnit.leadID }}</li>
     </ul>
     <h3>Cancelled apartment details:</h3>
     <ul  style="font-size: medium;">
      <li>Tapu Code :{{ unit.tapu_code }}</li>
      <li>Neighborhood :{{ unit.neighborhood }}</li>
      <li>Building :{{ unit.building_type }}</li>
      <li>FloorNO :{{ unit.floor }}</li>
      <li>Apartment description :<label dir="rtl"> {{ unit.description }}</label></li>
      <li>Collected Fees : {{ reservedUnit.collected_fees }}$</li>
      <li>Total price : {{ reservedUnit.total_price }}$</li>
     </ul>
     {% if reservedUnit.email_note != "" %}
         <h3>Notes:</h3>
         <pre style="font-size: medium; padding-left:40px; font-family:serif; font-weight: normal">{{reservedUnit.email_note}}</pre>
     {% endif %}

     <h3>
         Regards,<br>
         {{site.site_title}} Team
     </h3>
 </div>

{% endblock %}
