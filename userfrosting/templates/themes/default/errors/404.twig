{% extends "layouts/layout-simple.twig" %}

{# Set page properties (page.*) here. #}
{% block page %}
    {# By putting this in a special block, we ensure that it will be set AFTER the default values are set in the parent template, 
    but BEFORE the page itself is rendered. #}    
    {% set page = page | merge({
        "title"       : "404 Error",
        "description" : "We couldn't deliver.  We're sorry."
    }) %}
    {{ parent() }}
{% endblock %}
{% block content %}
    <div class="container text-center">
        <h1>Error 404</h1>
        <img class="img-fluid m-b-1 m-x-auto" src="{{site.uri.image}}/fry.png">
        <br><br>
        <p class="lead">Either way, click <a href="{{site.uri.public}}">here</a> to return to the front page.</p>
    </div> <!-- /container -->
{% endblock %}
{% block page_scripts %}

    <script>
        var url = window.location.href;
        console.log(url.indexOf('showContact1'))
        if(url.indexOf('showContact1') >0 || url.indexOf('showContact2')> 0 || url.indexOf('showContact3')> 0 || url.indexOf('ShowReceipt')>0 ){
            alert('لا يوجد عقد مسجل لهذه الشقة')
            window.location.href = site['uri']['public'] + '/unit';

        }
console.info(url)    </script>
{% endblock %}
